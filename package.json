{
  "name": "observer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "observer": "bin/observer.mjs",
    "observer-mcp": "bin/observer-mcp.mjs"
  },
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "test": "node --test",
    "syntax": "node --check bin/*.mjs && node --check src/*.mjs && node --check test/*.mjs",
    "check": "npm run syntax && git diff --check"
  }
}
